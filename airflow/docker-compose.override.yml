services:
  webserver:
    ports:
      - "8081:8080"
    volumes:
      - ../:/usr/local/airflow/project

  scheduler:
    volumes:
      - ../:/usr/local/airflow/project

  triggerer:
    volumes:
      - ../:/usr/local/airflow/project

  postgres:
    ports:
      - "5434:5432"
